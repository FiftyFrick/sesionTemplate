/*crear la BD siguiendo el pdf para la correcta funcionalidad del inicio de session y de registro

o 
*/
CREATE DATABASE login_system;

USE login_system;

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL
);


/*
usar solo si todavia no tiene realizado la pag. de registro la password esta cifrada con hashed_password
con el comando de javascript "pass.js"
*/

INSERT INTO users (username, password) VALUES ('user', 'pasword');
