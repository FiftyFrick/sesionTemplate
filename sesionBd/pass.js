password_hash('pasword', PASSWORD_BCRYPT);

/*
bash:

php -a

echo password_hash('password', PASSWORD_BCRYPT);


powershell:

php -r "echo password_hash('password', PASSWORD_BCRYPT);"

*/